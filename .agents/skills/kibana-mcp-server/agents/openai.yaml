interface:
  display_name: "Kibana Mcp Server"
  short_description: "Repo-specific patterns and workflows for kibana-mcp-server"
  default_prompt: "Use the kibana-mcp-server repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true