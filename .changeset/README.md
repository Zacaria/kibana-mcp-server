# Changesets

This repo uses Changesets for versioning and changelog generation.

## Common commands

```bash
npm run changeset
npm run changeset:version
```

Publishing is intentionally gated. Do not publish without completing the release checklist in
`docs/project/release-checklist.md`.
